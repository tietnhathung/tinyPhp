<html>
    <head>

    </head>
    <body>
        <foreach data="$data as $key => $value">
            <h1>{{$value}}</h1>
        </foreach>
        <foreach data="$data as $key => $value">
            <h2>{!$key!}</h2>
        </foreach>
    </body>
</html>
