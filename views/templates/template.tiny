<html>
    <head>

    </head>
    <body>
        <if parameters="is_array($data)">
            <foreach parameters="$data as $key => $value">
                <h1>{{$value}}</h1>
            </foreach>
            <else>
                <h1>Đây không phải mảng</h1>
            </else>
        </if>
        <switch parameters="3">
            <case parameters="5">
                {{"5"}}
                <break/>
            </case>
            <case parameters="2">
                {{"2"}}
                <break/>
            </case>
            <default>
                {{"default"}}
            </default>
        </switch>
    </body>
</html>
