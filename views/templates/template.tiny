<html>
    <head>

    </head>
    <body>
        <foreach data="$data as $key => $value">
            <h1><?php echo $value ?></h1>
        </foreach>
        <foreach data="$data as $key => $value">
            <h2><?php echo $key ?></h2>
        </foreach>
    </body>
</html>
